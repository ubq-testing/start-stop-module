import { Octokit } from "@octokit/rest";
import { Context } from "../types/context";

export async function pluginStartStop(context: Context, octokit: Octokit): Promise<object> {
  //

  return {};
}
